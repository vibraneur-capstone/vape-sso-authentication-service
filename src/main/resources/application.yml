server:
  port : 8821
  servlet:
    context-path: /${spring.application.name}/v1

spring:
  data:
    mongodb:
      uri: mongodb+srv://admin:admin@vape-sso-t9uut.mongodb.net/test?retryWrites=true&w=majority
      database: sso

com:
  vape:
    sso:
      jwt:
        issuer: vibraneur-sso-service
        signature-algorithm: HS256
        max-age: 1000000
        secret-key: CC4BBA2415B838D58193ADE9E492B
